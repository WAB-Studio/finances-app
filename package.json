{
  "name": "monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "npm run dev -w apps/finances",
    "build": "npm run build -w apps/finances",
    "typecheck": "npm run typecheck -w apps/finances",
    "lint": "npm run lint -w apps/finances",
    "check": "npm run check -w apps/finances"
  }
}
